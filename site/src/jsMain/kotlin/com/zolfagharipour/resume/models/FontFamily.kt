package com.zolfagharipour.resume.models

import com.zolfagharipour.resume.util.AppRes

enum class FontFamily(
    val font: String
) {
    Normal(font = AppRes.Font.NORMAL),
    Bold(font = AppRes.Font.BOLD)
}